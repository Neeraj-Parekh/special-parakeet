|---|---|---|---|---|---|
| 1 | 7-action allowlist in BoundedAgent | ✅ scripts/demo_agent.py:39-69 — all 7 actions present with correct costs (0/1/2/10/5/1/2), requires_approval flags, hard_caps dict for upi_circle_delegated_pay (max_per_txn=5000, max_per_month=15000, cooling_24h=5000, max_devices=5), auto_trigger_days=180 for revoke | ⚠️ PARTIAL — BoundedAgent only used in scripts/demo_agent.py:311 (demo main()). NOT imported in src/api/routes.py or any production path. Production enforcement uses verify_mandate() directly via X-Mandate header. Client-side cap pre-check (lines 106-116) only runs when someone manually executes the demo script. | ❌ No pytest asserts BoundedAgent.dispatch behavior. test_mandates.py never imports BoundedAgent. The 7-action allowlist is exercised only by manual `python scripts/demo_agent.py` runs. | **DECORATIVE** |
| 2 | device_id+user_id in HMAC chain | ✅ mandates.py:108-109 — payload["device_ids"]=list(device_ids); payload["user_id"]=user_id; lines 128-132 HMAC-signs the entire body including these fields | ✅ mandates.py:233-247 — verify_mandate checks device_id against payload["device_ids"] allowlist (BREACH "device_id_not_allowed") AND user_id against payload["user_id"] (BREACH "user_id_mismatch"). Both signed AND validated, not just logged. | ✅ test_upi_circle_device_id_not_allowed + test_upi_circle_user_id_mismatch + test_upi_circle_device_id_not_allowed_in_routes (end-to-end via API) | **REAL** |
| 3 | BH purpose code + mandate_type + device_id + user_id in audit | ✅ routes.py:653-658 — audit.log payload carries mandate_verdict, mandate_verdict_reason, mandate_type, bh_purpose_code, device_id, user_id. audit/logger.py:585-600 — Postgres INSERT has dedicated columns (mandate_type, bh_purpose_code, device_id, user_id) extracted from payload; file mode stores via `**payload` merge into body | ✅ audit body's hash chain (raw_hash = sha256(canonical(body) + prev_hash)) covers these fields since they're in the body. Postgres mode also indexes them in dedicated columns. | ✅ test_upi_circle_bh_purpose_code_in_audit asserts rec["mandate_type"], rec["bh_purpose_code"], rec["device_id"], rec["user_id"], rec["mandate_verdict"], rec["mandate_verdict_reason"] all surfaced | **REAL** |
| 4 | 12-value verdict_reason | ✅ mandates.py defines exactly 12 values: missing_mandate (L183), hmac_signature_mismatch (L188), expired_ttl (L193), inactivity_auto_revoke (L210), per_txn_cap_exceeded (L218), monthly_cap_exceeded (L227), device_id_not_allowed (L237), user_id_mismatch (L246), cooling_period_active (L263), ok (L270), amount_exceeds_max (L274), decode_error (L277) | ✅ Every value is used in actual verdict logic — each is the return value for a specific BREACH/EXPIRED/TAMPERED/REVIEW/VALID branch. NOT just an enum. | ✅ Tests assert 8 of the 12: ok (test_upi_circle_valid_delegation), missing_mandate (implicit), hmac_signature_mismatch (test_tampered_mandate_rejected, test_forged_mandate_header_rejected), expired_ttl (test_expired_mandate_rejected), per_txn_cap_exceeded, monthly_cap_exceeded, device_id_not_allowed, user_id_mismatch, cooling_period_active, inactivity_auto_revoke, amount_exceeds_max (test_over_limit_is_breach). Not directly asserted: decode_error (edge-case of malformed b64) — covered indirectly. | **REAL** |
| 5 | MandateVerdict.REVIEW cooling-period | ✅ mandates.py:135-150 — REVIEW class constant defined. mandates.py:254-264 — 24h cooling logic: prune recent txns older than 86400s, if any prior txn in window had amount >= cooling_24h_inr (default ₹5000), return REVIEW with verdict_reason="cooling_period_active" | ✅ routes.py:515-524 — when x_mandate present + verdict==REVIEW, sets decision="REVIEW", decision_source="mandate_review_required", creates a case (routes.py:617-625) in the review queue | ✅ test_upi_circle_cooling_period (mandate-level) + test_upi_circle_cooling_period_routes_to_review (API-level, asserts case_id is not None) | **REAL** |
| 6 | ₹5000/txn + ₹15000/month caps enforced | ✅ mandates.py:214-219 — per-txn cap: if amount_inr > max_per_txn (default 5000) → BREACH "per_txn_cap_exceeded". mandates.py:222-228 — monthly cap: if _cumulative_monthlyid] + amount_inr > max_per_month (default 15000) → BREACH "monthly_cap_exceeded". Cumulative counter mutated at L267 on VALID. Also BoundedAgent client-side pre-check at demo_agent.py:106-116 (returns "Action not permitted: exceeds per-txn cap"). | ✅ Both server-side checks run on every verify_mandate() call in /risk/score. In-memory cumulative dict persists across calls in same process. | ✅ test_upi_circle_per_txn_cap_exceeded (5001 → BREACH) + test_upi_circle_monthly_cap_exceeded (3×5000 + 500 → BREACH on 4th txn). NOTE: tests use 5001 not 6000, but the math is identical. | **REAL** |
| 7 | 5-device cap + 6-mo auto-revoke | ✅ mandates.py:106-107 — at mint, if len(device_ids)>5 → raise ValueError("OC-201B: max 5 devices per delegation"). routes.py:1310-1314 — duplicate check at API layer (422). mandates.py:205-211 — at verify, if now - _last_activityid] > inactivity_revoke_days*86400 (default 180) → EXPIRED "inactivity_auto_revoke". simulate_inactivity() helper at L306-320 backdates for testing. | ✅ Mint-time check is in issue_mandate() which is called by POST /v1/mandates. Verify-time check is in verify_mandate() which is called by POST /risk/score. _last_activity dict (L49) is mutated at L269 on VALID. | ✅ test_upi_circle_more_than_5_devices_rejected_at_mint (pytest.raises ValueError) + test_upi_circle_inactivity_auto_revoke (simulate_inactivity(m, 181) → EXPIRED) | **REAL** |
| 8 | test_mandates.py covers new action classes | ✅ 22 tests total; 13 new UPI Circle tests: valid_delegation, device_id_not_allowed, user_id_mismatch, per_txn_cap_exceeded, monthly_cap_exceeded, cooling_period, inactivity_auto_revoke, 5_devices_rejected, bh_purpose_code_in_audit, device_id_not_allowed_in_routes, cooling_period_routes_to_review, cod_order_backward_compat, decode_mandate_helper. | ✅ Pytest run: 22 passed in 8.79s. Tests assert verdict, verdict_reason, mandate_type, bh_purpose_code, decision, decision_source, case_id. | ✅ For UPI Circle: per_txn_cap (5001 blocked ✓), cooling_period (2nd txn within 24h → REVIEW ✓), auto_revoke (181 days → EXPIRED ✓), device_id rogue → BREACH ✓, 6-device mint → ValueError ✓. ❌ BUT: 3 new BoundedAgent actions (upi_circle_delegated_pay, validate_device_id, revoke_delegation_on_inactivity) NOT directly tested — BoundedAgent class is never imported in test_mandates.py. Tests verify the underlying verify_mandate() engine, not the agent class. | **REAL** (verify_mandate engine) + **DECORATIVE** (BoundedAgent class) |

## Honest verdict

- **REAL: 6 of 8** (Claims 2, 3, 4, 5, 6, 7)
- **DECORATIVE: 2 of 8** (Claims 1 and partially 8)
- **STUB: 0**
- **MISSING: 0**

The mandate engine itself (mandates.py + audit wiring + tests) is genuinely well-built and the prior session's Track D claims are substantively accurate for the verify_mandate() / OC-201B cap-enforcement story. The BoundedAgent class, however, is a demo-script-only artifact — it is NOT in the production code path (production routes.py uses verify_mandate() directly), and no pytest asserts its dispatch behavior. The 7-action allowlist is real code but its enforcement is decorative: the only thing that actually runs in production is the X-Mandate + X-Device-Id + X-User-Id header path through verify_mandate, which the tests cover thoroughly.

Also flagging: prior session claimed "63/63 total tests pass" — actual count is **117 passed + 8 skipped** in 37.46s. The project has grown well beyond 63 tests; the worklog entry is stale.

## Specific gaps for orchestrator

1. **scripts/demo_agent.py:72-147 (BoundedAgent class) — has no test coverage.** Add `tests/test_bounded_agent.py` that imports BoundedAgent, asserts dispatch("refund_order") → "Action not permitted", dispatch("upi_circle_delegated_pay", amount_inr=6000) → "exceeds per-txn cap", dispatch("upi_circle_delegated_pay", amount_inr=3000) → "I cannot perform this action. I have requested human approval." Right now the 7-action allowlist is a doc claim — the only thing that exercises it is a manual `python scripts/demo_agent.py` run.

2. **scripts/demo_agent.py:72 BoundedAgent is not in production path.** src/api/routes.py uses verify_mandate() directly via X-Mandate header. The "agent" in the production flow is whatever HTTP client presents the X-Mandate + X-Device-Id + X-User-Id headers — there is no Python-side agent enforcement of the 7-action allowlist on the server. The allowlist only runs client-side (in the demo). If the user's intent was server-side enforcement of "agent can only call N APIs", that's NOT what's happening — the server enforces mandate bounds but does NOT gate which actions the calling agent may attempt. Consider documenting this distinction explicitly in ARCHITECTURE.md or moving BoundedAgent into src/api/ as middleware.

3. **In-memory cumulative counters (mandates.py:47-49 _cumulative_monthly / _cumulative_24h / _last_activity) are NOT persisted.** They reset on every process restart, which means the ₹15,000/month cap and the 6-month inactivity auto-revoke are effectively unenforced across restarts. mandates.py:43-46 acknowledges this: "A real Postgres-backed implementation (Track E Day 2) would persist these in a ``mandate_counters`` table". Until that lands, the monthly cap and auto-revoke are real within a single process but reset on redeploy. The orchestrator should either (a) bump Track E Day 2 to add a `mandate_counters` table + migration, or (b) explicitly document this limitation in the architecture doc.

## Surprising findings

- The 24h cooling-period test (test_upi_circle_cooling_period) is cleverly written: it lowers `cooling_24h_inr=1000` and uses ₹1,500 then ₹500 to fire the gate without needing ₹5,000. This is real test design, not a stub.
- The `simulate_inactivity` helper (mandates.py:306-320) is a clean test affordance — backdates `_last_activity` for a given mandate ID. Good engineering, not a hack.
- `decode_mandate` (mandates.py:280-291) is correctly marked "Never use this for authorization decisions" — the codebase distinguishes inspect-only from verify paths.
- routes.py has a SECOND verify_mandate call at line 1623 (in /v1/simulate) that passes `device_id=None, user_id=None` — explicitly skipping UPI Circle device/user validation in the "what-if" explorer. Comment at lines 1626-1629 owns this trade-off. Honest code.
- The "63/63 tests pass" claim is stale — actual count is 117 passed + 8 skipped (likely Redis/Jaeger-dependent tests skipped in test env without those services).

---
Task ID: 10-c
Agent: Explore (DB+Merkle+dual-control cross-verifier)
Task: Cross-verify Track E + Track H claims (5 tables + 9 indexes, dual-mode fallback, register_model in lifespan, idempotency TTLCache, MerkleSealer RFC 6962, /v1/audit/{id}/proof, /v1/simulate dry-run, /v1/usage, dual-control override, test_db.py + 14 Track H tests) against actual code.

Work Log:
- Read worklog.md context (prior agents framed Microsoft Fabric benchmark; READ-ONLY scoping established)
- Read alembic/versions/001_initial.py — counted 5 CREATE TABLE statements (audit_records, cases, model_registry, idempotency_keys, psi_reference) + 9 CREATE INDEX statements (ix_audit_records_created_at, ix_audit_records_mandate_type_device_id, ix_cases_status, ix_cases_prediction_id, ix_cases_created_at, ix_model_registry_single_champion, ix_model_registry_is_champion, ix_idempotency_keys_expires_at, ix_psi_reference_feature). All via op.execute() raw SQL per "V3 rejected heavy ORMs" decision.
- Read alembic/versions/002_merkle_intervals.py — adds 6th table audit_merkle_intervals + 3 more indexes (ix_merkle_intervals_sealed_at, ix_merkle_intervals_root, ix_audit_records_interval) + per-record columns (interval_id, interval_position) on audit_records. IF NOT EXISTS guards make it idempotent.
- Read alembic/env.py — confirms DATABASE_URL is single source of truth; psycopg v3 connect, NOT SQLAlchemy.
- Read src/config.py — `is_postgres` property filters non-Postgres DSNs (line 86-90); file-mode fallback preserved when database_url is None.
- Read src/audit/logger.py (733 lines) — confirmed MerkleSealer class (lines 61-327) with: __init__ defaults (interval_size=1000, interval_seconds=3600), add() count-OR-time seal trigger (lines 131-136), seal() computes Merkle root with RFC 6962-style padding (lines 222-225: `leaves + [leaves[-1]] * (size - len(leaves))`), prev_interval_root chain (lines 158-163), per-record backfill UPDATE (lines 190-198), proof() returns sibling path from leaf to interval root (lines 238-327).
- Confirmed MerkleSealer IS wired into the audit write path: _log_postgres (line 614-626) calls self.sealer.add(record_id, raw_hash) AFTER the audit INSERT commit. NOTE: docstring claims "atomic with the audit INSERT" but the commit at line 612 happens BEFORE sealer.add at line 616 — NOT actually atomic; sealer failures don't roll back the audit INSERT.
- Read src/api/routes.py (1983 lines):
  * Confirmed lifespan (line 250) calls register_model at line 298 BUT only inside `if settings.is_postgres:` block (line 292) — file-mode (default test path) skips it. Claim "wired into lifespan" is technically true but misleading.
  * Confirmed idempotency cache: TTLCache(maxsize=settings.idem_maxsize, ttl=settings.idem_ttl_seconds) at line 246-248 in file mode; _idem_lookup_postgres + _idem_store_postgres (lines 1911-1969) for Postgres mode. REAL memory-leak fix.
  * Confirmed /v1/audit/{record_id}/proof at line 1543 — calls state["audit"].merkle_proof(record_id). Endpoint takes record_id:int (internal SERIAL id) NOT the user-facing audit_id string returned by /risk/score — usability gap (verifier needs DB-side id).
  * Confirmed POST /v1/simulate at line 1583 — explicitly NO audit.log()/cases.open_case()/stream.publish() calls in body (verified by grep — only `state["audit"].log`, `state["cases"].open_case`, `state["stream"].publish` occur at lines 617, 633, 700-722, 1016, 1407, 1438 — none in simulate range 1583-1779). dry_run forced True server-side (line 1609-1613).
  * Confirmed GET /v1/usage at line 1781 — returns AGGREGATE counts (docstring line 1795-1801 admits "multi-tenant merchant_id is not yet implemented — the counts are aggregate (all merchants combined)"). Per-merchant claim is FALSE; metering endpoint itself is REAL.
  * Confirmed /risk/{prediction_id}/override at line 1342 — dual-control path: validates BOTH admin keys (lines 1373-1378), REJECTS same-key (line 1384-1391), stores sha256-truncate-16 digests of each key separately (lines 1415-1418). NO HMAC chain — two independent digests, not HMAC of one keyed by the other. "HMAC chain" claim is FALSE.
- Read tests/test_db.py (277 lines, 6 tests: test_audit_log_to_postgres, test_case_lifecycle_postgres, test_model_registry_postgres, test_idempotency_postgres, test_hash_chain_postgres, test_audit_tail_postgres). ALL 6 tests SKIP unless DATABASE_URL points at a real Postgres DSN (pytestmark skipif at line 48). test_db.py:72-79 calls `alembic upgrade head` in the db_conn fixture — would surface migration failures but no explicit assertion on "5 tables + 9 indexes exist".
- Read tests/test_v3_endpoints.py (449 lines, 14 tests). Confirmed count = 14:
  1. test_merkle_root_padding_to_power_of_two (line 86) — pure-Python _merkle_root
  2. test_merkle_proof_reconstructs_root (line 105) — DECORATIVE: line 155 contains `or True` making the assert tautological
  3. test_merkle_proof_endpoint_requires_admin (line 170)
  4. test_merkle_proof_404_before_seal (line 177) — only tests 404 in file mode
  5. test_simulate_dry_run (line 195) — verifies audit tail unchanged
  6. test_simulate_admin_scope_also_works (line 233)
  7. test_simulate_mandate_breach_path (line 249)
  8. test_usage_admin_only (line 277)
  9. test_usage_returns_counts (line 284) — verifies counts shape, NOT per-merchant breakdown
  10. test_usage_since_hours_csv_parsing (line 319)
  11. test_dual_control_legacy_single_admin_still_works (line 337)
  12. test_dual_control_override_requires_two_keys (line 356)
  13. test_dual_control_same_key_rejected (line 377) — verifies same key → 400
  14. test_dual_control_two_different_keys_succeeds (line 397)
- Read docker-compose.yml — confirmed postgres (line 155) + redis (line 55) moved OUT of `["full"]` profile; api service unconditionally references DATABASE_URL.

Stage Summary:

## Verification Table

| # | Claimed feature | Code exists? | Actually wired? | Tests cover it? | Verdict |
|---|---|---|---|---|---|
| 1 | 5 tables + 9 indexes in Alembic | ✅ 001_initial.py:61-209 (5 CREATE TABLE + 9 CREATE INDEX via op.execute raw SQL) | ✅ alembic/env.py wires DATABASE_URL; test_db.py:72-79 fixture runs `alembic upgrade head` | ⚠️ test_db.py runs migrations but does NOT explicitly assert "5 tables + 9 indexes exist" (no information_schema query); all 6 test_db.py tests SKIP without DATABASE_URL=postgresql://... | REAL |
| 2 | Dual-mode Postgres + file fallback | ✅ config.py:75-90 (is_postgres property filters non-postgres DSNs); logger.py:359-393 branches on is_postgres | ✅ AuditLogger._log_postgres (line 561) vs _log_file (line 677); _idem_lookup_postgres vs state["idem"] TTLCache; CaseService + ModelRegistry also dual-mode | ✅ file-mode tests run by default (the 63 + 14 new); Postgres-mode covered by test_db.py (skipped without DATABASE_URL) | REAL |
| 3 | register_model in lifespan | ✅ routes.py:298-303 (register_model(...) call inside `async def lifespan`) | ⚠️ PARTIAL — only called inside `if settings.is_postgres:` (line 292); file mode (default test path) NEVER calls register_model from lifespan | ❌ No test exercises lifespan→register_model in Postgres mode; test_db.py:156-180 calls register_model directly, bypassing lifespan | PARTIAL — Postgres-only wiring, untested; file-mode still has the original "dead in prod" gap |
| 4 | Idempotency TTLCache + Postgres | ✅ routes.py:246-248 (TTLCache(maxsize=settings.idem_maxsize, ttl=settings.idem_ttl_seconds)); routes.py:1911-1969 (_idem_lookup_postgres + _idem_store_postgres INSERT into idempotency_keys with expires_at) | ✅ routes.py:423 (Postgres lookup) + 431 (file cache lookup); 805-814 (Postgres store); 427 (1% probabilistic cleanup via _idem_cleanup_postgres) | ✅ test_db.py:183-217 (test_idempotency_postgres) covers store/lookup/expiry; file-mode TTLCache covered by existing tests | REAL |
| 5 | MerkleSealer RFC 6962 + count-OR-time | ✅ logger.py:61-327 (MerkleSealer class); _merkle_root pads to power of 2 with last-leaf-repeat (lines 222-225); add() triggers on count OR elapsed (lines 131-136); seal() inserts audit_merkle_intervals row + chains prev_interval_root (lines 158-163, 167-184) | ✅ _log_postgres calls sealer.add(record_id, raw_hash) after every audit INSERT (line 614-626). NOTE: docstring claim "atomic with the audit INSERT" is INACCURATE — commit at line 612 happens BEFORE sealer.add at line 616; sealer failures don't roll back the audit INSERT | ⚠️ test_merkle_root_padding_to_power_of_two tests pure-Python _merkle_root only; test_merkle_proof_reconstructs_root is DECORATIVE (line 155 contains `or True` making the assert tautological — always passes regardless of correctness); NO test exercises count threshold, time threshold, OR prev_root chain. Postgres-path test would have to run in test_db.py but is skipped by default | REAL code, WEAK/DECORATIVE tests |
| 6 | /v1/audit/{id}/proof returns Merkle path | ✅ routes.py:1543-1581 (audit_proof endpoint) | ✅ calls state["audit"].merkle_proof(record_id) → AuditLogger.merkle_proof → MerkleSealer.proof (logger.py:238-327) which returns real proof dict (sibling path from leaf to interval root + interval chain anchor). USABILITY GAP: endpoint takes `record_id:int` (internal SERIAL PK), NOT the `audit_id` string returned by /risk/score; external verifier has no way to look up record_id from audit_id via the API | ⚠️ test_merkle_proof_endpoint_requires_admin (line 170) tests admin scope only; test_merkle_proof_404_before_seal (line 177) tests 404 in file mode only; NO test verifies a returned path actually reconstructs the root in Postgres mode | REAL code, WEAK tests, USABILITY GAP (record_id vs audit_id mismatch) |
| 7 | POST /v1/simulate (dry-run, no audit write) | ✅ routes.py:1583-1779 (simulate function); dry_run forced True server-side (lines 1609-1613) | ✅ verified by grep — NO state["audit"].log / state["cases"].open_case / state["stream"].publish calls in the simulate body (the 6 such calls in routes.py occur at lines 617, 633, 700-722, 1016, 1407, 1438 — none in range 1583-1779) | ✅ test_simulate_dry_run (line 195) verifies audit tail length unchanged before+after; test_simulate_mandate_breach_path (line 249) covers mandate routing | REAL |
| 8 | GET /v1/usage per-merchant counts | ✅ routes.py:1781-1857 | ❌ CLAIM IS FALSE — implementation is AGGREGATE counts across all merchants. Docstring line 1795-1801 admits "multi-tenant merchant_id is not yet implemented — the counts are aggregate (all merchants combined)". Merkle cadence IS surfaced (intervals_sealed_total, latest_interval). | ✅ test_usage_returns_counts (line 284) verifies counts shape but NOT per-merchant breakdown; test_usage_since_hours_csv_parsing covers input validation | STUB for per-merchant; metering endpoint itself is REAL but the claim wording is INACCURATE |
| 9 | Dual-control override (2 different admin keys, HMAC chain) | ✅ routes.py:1342-1455 (override endpoint); OverrideIn model (lines 159-173) | ⚠️ 2-key enforcement is REAL: lines 1373-1378 check both keys, line 1384 REJECTS same-key. HMAC chain claim is FALSE: lines 1415-1418 store two INDEPENDENT sha256-truncate-16 digests of each key — NOT an HMAC of one keyed by the other. No chaining, no HMAC. | ✅ test_dual_control_same_key_rejected (line 377) verifies same key → 400; test_dual_control_two_different_keys_succeeds (line 397) verifies 2 different keys → 200 + both digests in audit record; NO test for HMAC chain (because there isn't one) | REAL on 2-key enforcement + same-key rejection; "HMAC chain" claim is FALSE (MISSING) |
| 10 | test_db.py + Track H 14 tests cover the above | ✅ test_db.py (6 tests) + test_v3_endpoints.py (14 tests) | ⚠️ test_db.py: ALL 6 tests SKIP unless DATABASE_URL=postgresql://... (pytestmark skipif at line 48) — none run in default test invocation. test_v3_endpoints.py: 14 tests run in file mode, but test_merkle_proof_reconstructs_root (line 105) is DECORATIVE — line 155 contains `or True` making the assert tautological. NO test exercises MerkleSealer.add() count threshold, time threshold, prev_root chain, OR proof() method's actual output. NO test exercises lifespan→register_model path in Postgres mode. | n/a | PARTIAL — 14 tests exist + run by default; 6 Postgres tests exist but skip; 1 of 14 is tautological; major Merkle behaviors (count sealing, time sealing, prev_root chaining, proof output verification) are UNTESTED |

## Honest verdict: 4 of 10 REAL; 4 PARTIAL/STUB-with-inaccurate-claim; 2 WEAK-tests-but-real-code

Specific gaps for orchestrator:
1. **`/v1/usage` per-merchant claim is FALSE** (routes.py:1795-1801 docstring admits aggregate-only; merchant_id not wired into /risk/score → audit body)
2. **Dual-control "HMAC chain" claim is FALSE** (routes.py:1415-1418 stores two independent sha256-truncate-16 digests; no HMAC, no chaining between signature_1 and signature_2)
3. **test_merkle_proof_reconstructs_root is DECORATIVE** (test_v3_endpoints.py:155 contains `or True` making the assert always-pass; the follow-up `assert root == _merkle_root(leaves)` is tautological)
4. **register_model in lifespan is Postgres-only** (routes.py:292 — `if settings.is_postgres:` guard; file mode skips it; the original §A item 4 "dead in prod" gap remains for non-Postgres deployments)
5. **/v1/audit/{id}/proof usability gap** (routes.py:1543 — takes `record_id:int` internal SERIAL PK, NOT the user-facing `audit_id` string returned by /risk/score; no API endpoint to convert audit_id → record_id)
6. **Merkle sealing is NOT atomic with audit INSERT** (logger.py:612 commit happens BEFORE sealer.add at line 616; docstring at lines 373-377 + 605-609 claims "atomic with the audit INSERT above" — this is INACCURATE)
7. **Merkle count-OR-time sealing behavior is UNTESTED** — no test exercises MerkleSealer.add() to verify count threshold OR time threshold triggers a seal; no test verifies prev_interval_root chain; no test verifies the proof() method's actual output structure in Postgres mode
8. **test_db.py 6 tests SKIP by default** — without DATABASE_URL=postgresql://... the entire Postgres-path coverage is invisible (the dual-mode fallback is real but Postgres-path correctness is unverified in CI)

Surprising findings:
- The "HMAC chain" language in the prior session's worklog is not just unimplemented — there's no HMAC anywhere in the override path; the two admin keys are validated via plain API-key check_key lookup, then stored as independent sha256-truncate-16 digests. So even the redaction is sha256 (not HMAC). The chain exists only via the audit hash chain (raw_hash = sha256(canonical(body) + prev_hash)) — which is shared with every other audit record, not specific to the dual-control override.
- test_merkle_proof_reconstructs_root's `or True` is the kind of thing that suggests the test author hit a bug in the proof builder (the position bookkeeping in MerkleSealer.proof at logger.py:282-304 has a known asymmetry — sibling_idx parity at line 285 vs the proof.append at line 287-295 mixes left/right handling), couldn't make the test pass cleanly, and reached for `or True` as a placeholder. Worth flagging to the orchestrator — the proof builder's position bookkeeping may be wrong for odd leaf indices.
- The simulate endpoint body (routes.py:1620-1766) duplicates the entire /risk/score decision pipeline (~140 lines). The comment at line 1615-1619 admits this is intentional ("extracting a shared helper would touch Track C's decision section") — defensible for a hackathon but a maintenance liability.
- test_db.py:80-81 swallows ALL alembic failures with `except Exception: pass # pragma: no cover — assume schema is already applied`. So if `alembic upgrade head` fails for ANY reason (broken migration, bad DSN, perms), the tests silently proceed and likely fail later with confusing table-not-found errors rather than a clear migration error.


---
Task ID: 10-e
Agent: Explore (tests+CI+docs cross-verifier)
Task: Cross-verify prior session's claims (105 tests + 8 skipped, ci.yml 3 jobs, mlops.yml 7-stage TFX, 5 helper scripts, README/PITCH_SCRIPT/ARCHITECTURE/MODEL_CARD/RESEARCH/API_SPEC line counts + content, alert_rules.yml 5 rules, alertmanager.yml, V2+V3 banners) against actual code.

Work Log:
- Read worklog prior context (Task 10-e scope: READ-ONLY verification, no code changes).
- LS /home/z/my-project/upload/RTO_Trust_Layer_FULL root → confirmed 11 test files, 13 scripts, 7 docs MD, .github/workflows/{ci.yml, mlops.yml}, monitoring/{alert_rules.yml, alertmanager.yml} all present.
- Ran `python3 -m pytest tests/ --collect-only -q` → 125 tests collected.
- Ran `python3 -m pytest tests/ -v` → **117 passed + 8 skipped in 20.07s** (NOT 105+8 as claimed — actual is 117+8).
- Skipped breakdown (8 total): 6 in test_db.py (DATABASE_URL not set), 2 in test_streaming.py (REDIS_URL not set) — matches "Postgres+Redis path" claim.
- Read .github/workflows/ci.yml (238 lines) — verified 3 jobs (lint-test, docker-build, load-test), Postgres+Redis services block (lines 44-66), `alembic upgrade head` (line 127), group-leakage gate with assert leak==0 (lines 116-119), Trivy action severity=CRITICAL,HIGH + exit-code:1 (lines 185-191), grafana/k6-action@v0.3.1 with filename tests/load/risk_api_load.js (lines 227-233).
- Read .github/workflows/mlops.yml (435 lines) — verified 7 stages (data-analysis, data-validation, model-training, model-gate, container-build, deploy-staging, monitor). PR-AUC≥0.60 gate is a REAL `if pr_auc < 0.60: sys.exit(1)` Python heredoc (lines 199-210). Canary invokes scripts/canary_gate.py (line 300). Slice-metrics invokes scripts/slice_metrics.py (line 307). Stage 6 deploy-staging just `echo "Blue-green deploy..."` (DECORATIVE — no kubectl/helm). Stage 7 monitor runs real `python scripts/check_error_rate.py` (line 422) but the rollback step is just `echo "::warning::ROLLBACK..."` (DECORATIVE — no actual kubectl rollout undo).
- LS scripts/ — 5 NEW CI helper scripts confirmed: canary_gate.py (252 lines), check_error_rate.py (170 lines), profile_data.py (205 lines), slice_metrics.py (224 lines), validate_data.py (275 lines). All substantive Python with argparse + main functions, NOT stubs.
- Read README.md (208 lines) — hero/prob/solution/quick-start/results/docs/identity all present, BUT lines 68-70 + line 122 say "93 tests pass" — STALE NUMBER (actual is 117). Documentation drift.
- Read docs/PITCH_SCRIPT.md (192 lines) — Act 1 (0:00-0:45), Act 2 (0:45-4:00), Act 3 (4:00-5:00), word-for-word with italic stage directions, time-stamped throughout. BUT lines 45 + 176 say "Ninety-three tests pass" — STALE.
- Read docs/ARCHITECTURE.md (654 lines — EXACT MATCH to claim) — 3 Mermaid diagrams (lines 47, 155, 366), 10x/100x/1000x scaling analysis (section 7, lines 267-351) with concrete triggers and fix paths. Confirmed V2+V3 superseded banners reference at top (lines 3-9).
- Read docs/MODEL_CARD.md (403 lines — claim said 381, UNDERSTATED by 22) — 9 sections matching Google Mitchell et al. 2019 spec (Model details, Intended use, Training data, Evaluation, Explainability, Limitations, Bias analysis, Ethical considerations, Caveats). is_cod reframed (lines 119-131): "gates model invocation... pass-through for logging... carries no predictive signal beyond 'the model was invoked'" — REAL reframe, not just acknowledgment.
- Read docs/RESEARCH.md (295 lines — EXACT MATCH) — 5 papers cited (Papers 1-5: E-Commerce Fraud SLR, Credit Card Threshold Opt, Building Trust in Agentic Commerce, COD Fraud Indian, AI Agent Risks & Guardrails). **BUT only 2/5 have actual DOIs** (Paper 1: doi.org/10.26599/BDMA.2024.9020015; Paper 2: doi.org/10.3390/math14010021). Papers 3, 4, 5 are vendor industry briefs (Liminal, Pragma, Atlan) with URL links, NOT DOIs. Claim "5 papers w/DOIs" is INFLATED — should be "5 papers, 2 with DOIs, 3 with URL citations". Also: line 270 says "93/93 tests pass" — STALE.
- Read docs/API_SPEC.md (1385 lines — claim said 1239, UNDERSTATED by 146) — 22 endpoints (sections 3.1-3.22), 10 tags (Risk, Audit, Rules, Cases, Models, Policy, Mandates, Feedback, Metering, Health), real JSON request+response bodies with field-by-field tables (sample at lines 106-185 for POST /risk/score).
- Read monitoring/alert_rules.yml (53 lines) — exactly 5 rules: CircuitBreakerOpen (`rto_circuit_state == 2`), DriftDetected (`rto_drift_ddm_state == 2 or rto_drift_adwin_state == 2`), AuditWriteErrors (`rate(rto_audit_write_errors_total]) > 0`), HighRtoRate (`rate(risk_decisions_total{decision="REJECT"}]) / clamp_min(rate(risk_decisions_total]), 0.001) > 0.5`), StreamConsumerDown (`up{job="stream-worker"} == 0 or up{job="stream-processor"} == 0 or up{job="drift-consumer"} == 0`). Real PromQL.
- Read monitoring/alertmanager.yml (39 lines) — route config (default → critical → warning subroutes via matchers), 3 receivers (default, critical, warning — all webhook_configs), inhibit_rules (critical suppresses warning on equal component).
- Read first 10 lines of docs/ARCHITECTURE_V2.md + docs/ARCHITECTURE_V3.md — V2 banner (line 1): "HISTORICAL — superseded by V3..."; V3 banner (line 1): "AUTHORITATIVE — this is the current engineering architecture...". Both REAL.

Stage Summary:

| # | Claimed feature | Code exists? | Actually wired? | Tests cover it? | Verdict |
|---|---|---|---|---|---|
| 1 | 105 tests + 8 skipped | ✅ tests/ (11 files, 125 collected) | ✅ pytest run → 117 passed + 8 skipped in 20.07s | N/A | REAL but COUNT UNDERSTATED (105 vs actual 117) |
| 2 | ci.yml 3 jobs + Postgres+Redis+Alembic+leakage gate+Trivy+k6 | ✅ .github/workflows/ci.yml:34,154,201 | ✅ services block lines 44-66; alembic upgrade head line 127; group-leakage assert leak==0 lines 116-119; Trivy severity=CRITICAL,HIGH+exit-code=1 lines 185-191; k6-action@v0.3.1 lines 227-233 | N/A | REAL |
| 3 | mlops.yml 7-stage TFX + PR-AUC≥0.60 gate + canary + blue-green | ✅ .github/workflows/mlops.yml:74,106,136,253,320,357,394 | ✅ 7 stages; PR-AUC gate is real `if pr_auc < 0.60: sys.exit(1)` heredoc lines 199-210; canary_gate.py + slice_metrics.py invoked lines 300,307; ❌ Stage 6 deploy-staging is `echo "Blue-green deploy..."` line 374-377 (NO kubectl/helm); ❌ Stage 7 rollback is `echo "::warning::ROLLBACK..."` lines 429-434 (NO actual kubectl rollout undo) | N/A | PARTIAL — pipeline scaffolding REAL, blue-green deploy + auto-rollback are DECORATIVE |
| 4 | 5 helper scripts | ✅ scripts/canary_gate.py(252L), check_error_rate.py(170L), profile_data.py(205L), slice_metrics.py(224L), validate_data.py(275L) | ✅ each is substantive Python with argparse+main, NOT stubs | N/A | REAL |
| 5 | README product landing page | ✅ README.md (208 lines) | ✅ hero/prob/solution/quick-start/results/docs/identity all present | N/A | REAL but STALE TEST COUNT (lines 68-70, 122 say "93 tests pass"; actual is 117) |
| 6 | PITCH_SCRIPT 3-act word-for-word | ✅ docs/PITCH_SCRIPT.md (192 lines) | ✅ Act 1 (0:00-0:45) line 11; Act 2 (0:45-4:00) line 36; Act 3 (4:00-5:00) line 163; word-for-word text + italic stage directions + time-stamps | N/A | REAL but STALE TEST COUNT (lines 45, 176 say "Ninety-three tests pass"; actual is 117) |
| 7 | ARCHITECTURE 654 lines + Mermaid + scaling | ✅ docs/ARCHITECTURE.md (654 lines — EXACT MATCH) | ✅ 3 Mermaid diagrams (lines 47, 155, 366); 10x/100x/1000x scaling analysis section 7 (lines 267-351) with concrete triggers + fix paths | N/A | REAL |
| 8 | MODEL_CARD 381 lines + Google spec + is_cod reframed | ✅ docs/MODEL_CARD.md (403 lines — claim UNDERSTATED by 22) | ✅ 9 sections matching Google Mitchell 2019 spec; is_cod reframed lines 119-131 ("pass-through for logging... carries no predictive signal beyond 'the model was invoked'") | N/A | REAL |
| 9 | RESEARCH 295 lines + 5 papers w/DOIs | ✅ docs/RESEARCH.md (295 lines — EXACT MATCH); 5 papers listed | ⚠️ Only 2/5 papers have actual DOIs (Paper 1: doi.org/10.26599/BDMA.2024.9020015; Paper 2: doi.org/10.3390/math14010021). Papers 3-5 (Liminal, Pragma, Atlan industry briefs) have URL links, NOT DOIs. | N/A | PARTIAL — line count + 5 papers REAL; "w/DOIs" claim INFLATED (only 2/5 have DOIs) |
| 10 | API_SPEC 1239 lines + 22 endpoints + examples | ✅ docs/API_SPEC.md (1385 lines — claim UNDERSTATED by 146); 22 endpoints (3.1-3.22); 10 tags (Risk/Audit/Rules/Cases/Models/Policy/Mandates/Feedback/Metering/Health) | ✅ Real JSON request+response bodies with field-by-field tables (sample lines 106-185 for POST /risk/score) | N/A | REAL |
| 11 | alert_rules.yml 5 rules | ✅ monitoring/alert_rules.yml (53 lines) | ✅ exactly 5 rules: CircuitBreakerOpen, DriftDetected, AuditWriteErrors, HighRtoRate, StreamConsumerDown — all with real PromQL expressions | N/A | REAL |
| 12 | alertmanager.yml routing | ✅ monitoring/alertmanager.yml (39 lines) | ✅ route config (default → critical → warning subroutes); 3 receivers (default/critical/warning all webhook_configs); inhibit_rules (critical suppresses warning on equal component) | N/A | REAL |
| 13 | V2+V3 superseded banners | ✅ docs/ARCHITECTURE_V2.md + ARCHITECTURE_V3.md | ✅ V2 banner line 1 "HISTORICAL — superseded by V3..."; V3 banner line 1 "AUTHORITATIVE — this is the current engineering architecture..." | N/A | REAL |

- Honest verdict: **11 of 13 fully REAL; 2 PARTIAL (Claim 3 deploy/rollback decorative; Claim 9 DOIs inflated); 0 STUB; 2 DECORATIVE sub-items within Claim 3; 0 MISSING.**
- Specific gaps for orchestrator:
  1. **Stale test count across 4 docs**: README.md:68,122 + PITCH_SCRIPT.md:45,176 + RESEARCH.md:270 all say "93 tests pass"; actual is 117 passed + 8 skipped (per `pytest tests/ -v` run in sandbox). The worklog claim of "105 passed" is ALSO stale (latest pytest collection is 125 tests total = 117 passing + 8 skipped). Recommend orchestrator issue a single search-and-replace commit `s/93 tests pass/117 tests pass/g` across README+PITCH_SCRIPT+RESEARCH + update worklog narrative.
  2. **mlops.yml Stage 6 (deploy-staging) is decorative** (.github/workflows/mlops.yml:374-377): the "Blue-green deploy of ghcr.io/..." step is `echo "Blue-green deploy..."` — no `kubectl set image` / `helm upgrade`. ARCHITECTURE.md:413 markets this as "blue-green to staging on main; old replica kept live for 5 min for fast rollback" but the actual deploy is a placeholder. Either wire a real `kubectl`/`helm` step OR rephrase the architecture doc + comment in mlops.yml to make the placeholder explicit.
  3. **mlops.yml Stage 7 rollback is decorative** (.github/workflows/mlops.yml:429-434): the "Rollback on failure" step is `echo "::warning::ROLLBACK: error rate exceeded..."` — no `kubectl rollout undo` / `helm rollback`. ARCHITECTURE.md:414 markets this as "auto-rollback if error > 1% for 3 consecutive evaluations". The Prometheus query (check_error_rate.py:170L, real) DOES run, but the rollback action itself is a no-op echo. Recommend either wire real rollback or add a TODO comment + flag in worklog.
  4. **RESEARCH.md DOI claim inflated**: claim says "5 pitch papers with DOIs" but only 2/5 actually have DOIs (Papers 1+2 academic). Papers 3, 4, 5 are industry analyst briefs (Liminal, Pragma, Atlan) cited by URL only. RESEARCH.md:106,159,217 show vendor URLs, not DOIs. Recommend reframe to "5 pitch papers (2 with DOIs, 3 with URL citations)".
  5. **Counting inconsistency** (understated, not inflated): Claim 1 said "105 tests" (actual 117); Claim 8 said "381 lines" (actual 403); Claim 10 said "1239 lines" (actual 1385). All three are understated — the prior session either captured an earlier snapshot or rounded down. Cosmetic issue.

---
Task ID: 10-a
Agent: Explore (cost-optimizer cross-verifier)
Task: Cross-verify prior session's Track C + Track N claims (cost-optimizer wiring, Bahnsen Eq.(6), /v1/policy/cost-curves bootstrap, 5-way intervention, demo keys removed, test_ship.py asserts) against actual code. READ-ONLY.

Work Log:
- Read worklog.md prior context (Task ID 3-b Track C + Task ID 4-N Track N worklog entries).
- Read src/business/cost_optimizer.py in full (729 lines): optimal_decision (lines 86-162), optimal_intervention (lines 169-252), calibrate_probabilities (lines 259-344), cost_curve_sweep (lines 355-437), bootstrap_cost_ci (lines 440-548), find_cost_crossover (lines 551-611), intervention_curve_sweep (lines 618-657), find_intervention_crossover (lines 660-728).
- Read src/api/routes.py (1984 lines) — focused on the /risk/score decision path (lines 460-680), the /v1/policy/cost-curves endpoint (lines 1047-1259), the /v1/simulate mirror (lines 1640-1748), and module-level constants ACCEPT_T/REJECT_T + DEFAULT_COST_WEIGHTS + DEFAULT_COST_CURVE_RESAMPLES (lines 65-104).
- Grep'd routes.py for all optimal_decision / optimal_intervention / calibrate_probabilities / ACCEPT_T / REJECT_T / intervention sites — confirmed optimal_decision IS the live decision (routes.py:566, 1701), optimal_intervention IS the live intervention recommendation (routes.py:578, 1705), calibrate_probabilities is IMPORTED but NEVER CALLED (routes.py:32 import only).
- Read src/audit/logger.py (lines 550-590) — confirmed the audit logger is generic (`**payload` spread at line 575) so any field passed in state["audit"].log({...}) lands in the JSONB body; the intervention + intervention_costs fields are passed in via routes.py:650-651.
- Read dashboard/index.html in full (205 lines) — confirmed `kScore` + `kAdmin` inputs use type="password" + placeholder with NO `value=` attribute (lines 42-43); live fetch from /v1/policy/cost-curves wired at line 111 with loading/error states (lines 113-138).
- Read tests/test_ship.py in full (737 lines, 31 test functions) — counted 12 Track N tests (per worklog claim) + 4 Track C tests + earlier optimal_decision/calibrate_probabilities/cost_curve_sweep/bootstrap_cost_ci/find_cost_crossover tests.
- Ran pytest tests/test_ship.py → 31/31 PASSED in 10.71s.
- Ran pytest tests/ → 117 passed + 8 skipped in 23.13s (Postgres/Redis path skips, unchanged).

Stage Summary:

Verification table:

| # | Claimed feature | Code exists? | Actually wired? | Tests cover it? | Verdict |
|---|---|---|---|---|---|
| 1 | Cost-optimizer wired into decision | ✅ routes.py:566 `decision, costs = optimal_decision(proba, **DEFAULT_COST_WEIGHTS)` (also /v1/simulate at routes.py:1701) | ✅ YES — `optimal_decision()` return value IS the `decision` field. The static `ACCEPT_T, REJECT_T = 0.15, 0.60` at routes.py:72 still EXIST in code but are NOT consulted in the decision path; they're only surfaced as `legacy_accept_t`/`legacy_reject_t` at routes.py:750-751 for backward-compat display. Comment at routes.py:744: "the live decision path no longer consults them; it consults `optimal_decision(p, weights=...)`." | ✅ YES — `test_decision_uses_cost_optimizer_not_static_thresholds` (test_ship.py:271-313) asserts `body["decision_source"] == "cost_optimal_bmr"` AND `body["decision"] == optimal_decision(body["probability"])[0]` AND `body["policy_hint"] == expected_decision` AND cost_breakdown values match within ±0.5. `test_decision_uses_cost_optimizer_with_review_rule_gate` (test_ship.py:315-344) covers the rule-gate interaction. | **REAL** |
| 2 | Bahnsen Eq.(6) calibrate_probabilities | ✅ cost_optimizer.py:259-344 — correctly implements `P*(f\|x) = P(f\|x)·P_orig/P_und` with edge cases (p_und=0 → all zeros, p_orig=0 → all zeros, [0,1] clipping, NaN propagation, no-op fast path when p_und == p_orig). | ❌ NOT WIRED — imported in routes.py:32 but NEVER CALLED outside tests. The `optimal_decision(proba, ...)` call at routes.py:566 passes the raw `proba` from the model directly, without recalibrating per Eq.(6). Grep across src/ confirms zero call sites in routes.py, scripts/, or src/models/. The docstring at cost_optimizer.py:100 even says "Use calibrate_probabilities first if training resampled the minority class" — but no caller does. | ✅ YES (function-level) — `test_calibrate_probabilities_bahnsen_eq6_noop_when_priors_equal` (test_ship.py:47) + `test_calibrate_probabilities_bahnsen_eq6_rescales_when_undersampled` (test_ship.py:59, 7 edge-case assertions). These test the function in isolation, not its integration into the live path. | **STUB** — function exists + is mathematically correct + has unit tests, but is dead code in the live API. If SMOTE/under-sampling is ever wired into the training pipeline (per the docstring), the live `optimal_decision` path would still use inflated probabilities. |
| 3 | /v1/policy/cost-curves with bootstrap CIs | ✅ routes.py:1047 (`@app.get("/v1/policy/cost-curves")`) — default `n_resamples=DEFAULT_COST_CURVE_RESAMPLES=500` (routes.py:103). Calls `cost_curve_sweep(...)` (routes.py:1126) and `bootstrap_cost_ci(...)` (routes.py:1136). The bootstrap implementation at cost_optimizer.py:519-528 preserves row marginals per Drummond-Holte §3.6 — `pos_sample = [pos_p[rng.randrange(n_pos)] for _ in range(n_pos)]` and `neg_sample = [neg_p[rng.randrange(n_neg)] for _ in range(n_neg)]`. | ✅ YES — returns full threshold sweep (19 points × 0.05 step), bootstrap CI dict keyed by str(threshold), optimal_threshold = argmin of cost. 503 if model unloaded or no labeled data. 422 if n_resamples out of [1, 5000]. | ✅ YES — `test_cost_curves_endpoint_returns_valid_structure` (test_ship.py:347-376), `test_cost_curves_endpoint_503_when_model_unloaded` (test_ship.py:379), `test_bootstrap_cost_ci_returns_band_per_threshold` (test_ship.py:101-114). | **REAL** (minor caveat: dashboard fetches with `?n_resamples=100` at dashboard/index.html:111, so the user-facing dashboard only uses 100 resamples — endpoint default is 500 but the production dashboard opts for 100 for UX). |
| 4 | 5-way intervention (Track N) | ✅ cost_optimizer.py:169-252 (`optimal_intervention(p, amount_inr, weights)`) — actually computes argmin over `{ship, otp_verify, partial_cod, address_check, hold}`. Cost formulas (cost_optimizer.py:235-245) match the docstring math exactly: `ship = p·amt`, `otp_verify = c_otp + (1-eff_otp)·p·amt`, `partial_cod = c_partial_cod + (1-eff_cod)·p·amt`, `address_check = c_address_check + (1-eff_addr)·p·amt`, `hold = c_hold + c_hold_residual_ship_rate·p·amt`. Pragma 2025 effectiveness rates used at DEFAULT_INTERVENTION_WEIGHTS (cost_optimizer.py:66-79): OTP 0.82, partial COD 0.65, address check 0.45, hold residual ship rate 0.30. | ✅ YES — routes.py:578 calls `intervention, intervention_costs = optimal_intervention(proba, order.amount_inr)`. The per-amount FN cost (`amt = float(amount_inr)`) IS the Bahnsen Eq.(5) headline — used in the 5-way path. | ✅ YES — 6 tests assert real argmin math, not just shape: `test_optimal_intervention_ship_at_low_p` (p*amt<6.10 → ship), `test_optimal_intervention_otp_at_medium_p` (verifies exact OTP cost formula), `test_optimal_intervention_at_high_p` (default weights → otp_verify; re-tuned weights c_hold=1+residual=0.01 → hold), `test_per_amount_fn_cost_changes_optimal_intervention` (same p, different amt → different intervention), `test_optimal_decision_per_amount_fn_cost_flag` (constant c_fn=600 vs amount_inr=600 agree; amount_inr=52000 → REJECT), `test_intervention_curve_sweep_returns_per_threshold_data` + `test_find_intervention_crossover_returns_regions` (verifies ship→otp_verify crossover at p=0.10 for amt=100). | **REAL** |
| 5 | intervention + intervention_costs in /risk/score response + audit | ✅ routes.py:764-765 (response body has `intervention` + `intervention_costs`) + routes.py:650-651 (audit payload has both fields) + routes.py:1705-1707 (mirror in /v1/simulate). Audit logger is generic — `**payload` spread at logger.py:575 — so any field passed via `state["audit"].log({...})` lands in the JSONB body. | ✅ YES — both fields populated by the `optimal_intervention(proba, order.amount_inr)` call at routes.py:578; the audit comment at routes.py:643-651 explicitly states the audit captures the BMR-vs-execution gap. | ✅ YES — `test_intervention_costs_in_response` (test_ship.py:595-626) asserts all 3 fields present, `intervention ∈ INTERVENTIONS`, `set(intervention_costs.keys()) == set(INTERVENTIONS)`, AND cross-checks the response `intervention` IS the argmin of `intervention_costs` (consistency assertion). `test_intervention_response_reflects_per_amount_fn_cost` (test_ship.py:629-664) verifies ship's cost = p*amount (Bahnsen Eq.(5)). | **REAL** |
| 6 | Default demo keys removed | ✅ dashboard/index.html:42-43 — `<input id="kScore" type="password" placeholder="Enter scorer key" autocomplete="off"/>` and `<input id="kAdmin" type="password" placeholder="Enter admin key" autocomplete="off"/>` — NO `value=` attribute. The only `value=` attributes remaining in dashboard are non-secret form defaults (oid, amt, po, pr — order test data). | ✅ YES — confirmed by grep: only API-key inputs use type="password"; the previous `value="score-demo-key"` is gone. Live fetch wired at dashboard/index.html:106-138 with loading + 401/503/HTTP error states. | N/A — no automated tests for the dashboard (vanilla HTML/JS). | **REAL** |
| 7 | test_ship.py asserts cost-optimizer path | ✅ tests/test_ship.py:271-313 `test_decision_uses_cost_optimizer_not_static_thresholds` — explicitly asserts `body["decision_source"] == "cost_optimal_bmr"` (NOT static-threshold source), `body["decision"] == expected_decision` where `expected_decision, _ = optimal_decision(body["probability"])` (function-call cross-check), `body["policy_hint"] == expected_decision` (legacy field consistency), `body["cost_breakdown"][action]` ≈ `expected_costs[action]` for all 3 actions (cost-math cross-check). | ✅ YES — the test goes well beyond shape-checking; it cross-validates that the response decision equals `optimal_decision()` output for the actual model probability emitted on the request. The test docstring even says: "before this change, the cost-optimizer was stored as `policy_hint` but the actual decision came from `if proba < 0.15: ACCEPT`. After this change, `decision` *is* the cost-optimizer output." | ✅ YES — 31 tests in test_ship.py total; 117 passed + 8 skipped across the whole suite. | **REAL** |

Honest verdict: **6 of 7 claims are REAL; 1 is STUB; 0 are DECORATIVE; 0 are MISSING.**

The single STUB is Claim #2 — `calibrate_probabilities()` (Bahnsen Eq.(6)) is fully implemented (cost_optimizer.py:259-344), mathematically correct, and unit-tested, but is **imported in routes.py:32 and NEVER CALLED** anywhere in the live API path. It is dead code. If SMOTE/under-sampling is ever wired into the training pipeline (per its own docstring's instructions), the live `optimal_decision()` path would still use the inflated raw probabilities — the calibration step that Bahnsen Eq.(6) is supposed to undo would never fire.

Top-3 specific gaps for orchestrator to address:

1. **`calibrate_probabilities()` is dead code** — file: `src/api/routes.py:32` (import), `src/business/cost_optimizer.py:259` (definition). Either (a) wire `proba = calibrate_probabilities(proba, p_orig, p_und)` into the live decision path (with `p_und`/`p_orig` derived from the trained model's metadata + the original training prior — would require storing those priors in the model artifact), or (b) remove the import to avoid the decorative-API smell. Currently the API surface pretends to do Bahnsen Eq.(6) recalibration when it doesn't.

2. **The headline Bahnsen Eq.(5) per-amount FN cost does NOT influence the primary ACCEPT/REVIEW/REJECT decision** — file: `src/api/routes.py:566` calls `optimal_decision(proba, **DEFAULT_COST_WEIGHTS)` WITHOUT `amount_inr=order.amount_inr`. The 3-way path uses `c_fn=600` constant; only the 5-way `optimal_intervention(proba, order.amount_inr)` at routes.py:578 gets the per-amount FN cost. This is documented as intentional (Track N worklog line 661: "the 3-way optimal_decision() call in routes.py KEEPS the constant c_fn=600... backward compat with Track C's tests"), but it means the cost-optimal ACCEPT/REVIEW/REJECT is the same for a ₹600 order and a ₹52,000 order at the same probability — the opposite of the Bahnsen 2013 paper's headline. The `amount_inr` parameter exists in `optimal_decision()` signature (cost_optimizer.py:93) but is NEVER PASSED in the live 3-way call. Recommend either: (a) pass `amount_inr=order.amount_inr` to the 3-way `optimal_decision` call (would require updating Track C tests that assert `cost_breakdown[action] == optimal_decision(p)[1][action]` without amount_inr), or (b) explicitly document this limitation in API_SPEC.md.

3. **Dashboard uses `n_resamples=100`, not the ≥500 default the endpoint honors** — file: `dashboard/index.html:111` — `fetch("/v1/policy/cost-curves?n_resamples=100", ...)`. The endpoint default at routes.py:103 is `DEFAULT_COST_CURVE_RESAMPLES = 500` and the bootstrap_cost_ci docstring (cost_optimizer.py:467) explicitly says "≥500 recommended by Drummond-Holte skill.yaml." The production dashboard opts for 100 (probably for the 3-5 sec → 0.6 sec latency trade-off). Either (a) bump the dashboard to n=500 with a loading spinner, or (b) document that the user-facing dashboard uses 100 for UX and only programmatic consumers get the 500 default. The "≥500 bootstrap CIs" claim is true at the endpoint level but misleading at the dashboard UX level.

Surprising findings:

- **The 3-way `optimal_decision()` is NOT given `amount_inr` in the live path** — `routes.py:566` and `routes.py:1701` both call `optimal_decision(proba, **DEFAULT_COST_WEIGHTS)` without `amount_inr`. So per-amount FN cost (Bahnsen Eq.(5)) influences ONLY the 5-way `intervention` recommendation (routes.py:578, 1705), NOT the primary ACCEPT/REVIEW/REJECT decision. The Bahnsen 2013 paper's headline (per-amount FN cost) is half-wired at best.

- **The static `ACCEPT_T, REJECT_T = 0.15, 0.60` constants are NOT removed** — they're shadowed (defined at routes.py:72, surfaced at routes.py:750-751 as `legacy_accept_t`/`legacy_reject_t` for backward-compat in the `gate_thresholds` response field). The worklog claim "replaces static 0.15/0.60" is real in the sense that the decision path no longer consults them; the constants themselves still exist in the source. This is a quibble, not a defect — the comment at routes.py:744 makes the intent explicit.

- **Track N's `optimal_intervention` tests DO assert real argmin math, not just shape** — `test_optimal_intervention_at_high_p` (test_ship.py:457-498) is particularly strong: it verifies both (a) default weights → otp_verify wins (because eff_otp=0.82 > eff_hold_residual=0.30), AND (b) re-tuned weights with c_hold=1 + residual=0.01 → hold wins. This proves the argmin math is genuinely conditional on weights, not hardcoded. `test_per_amount_fn_cost_changes_optimal_intervention` (test_ship.py:501-519) verifies the same p produces different interventions at different amounts — the Bahnsen Eq.(5) headline property.

- **The dashboard fetches with `n_resamples=100`, not 500** — `dashboard/index.html:111`. The endpoint's default is 500 (routes.py:103), and the bootstrap_cost_ci docstring says ≥500 is recommended per Drummond-Holte skill.yaml. The dashboard's 100 is a UX trade-off (faster load), but it means the user-facing cost-curve explorer is showing wider CIs than the endpoint's default would.

- **117/125 tests pass** (8 skipped are Postgres-path test_db.py + Redis-path test_streaming.py). The worklog claimed "105/105 pass" at Track L Day 4 — that was an older state. Current count is 117 passed + 8 skipped = 125 total test functions. The Track N "12 new tests" claim is accurate (test_ship.py grew by 12 tests covering optimal_intervention + per-amount FN cost + intervention_costs in response + intervention_curves endpoint + amount_inr param + 422 validation).

---
Task ID: 10-synthesis
Agent: Z.ai Code orchestrator (cross-verification synthesis)
Task: User re-pasted the prior conversation (Tier 1-4 Q&A + product vision + Microsoft Fabric benchmark + paper-studied verification + 5 Missions + 3-Act pitch + 4-Question Gate) and asked: "analyze, cross-compare what is done, what needs to be cross-verified, what needs to be done more seriously more properly, and how you can help." User directive: primary focus should be paper-skills + tech stack + code path, NOT web design first. Launched 5 parallel Explore subagents (10-a through 10-e) to cross-verify the prior session's claims against the actual Python code.

Work Log:
- Read worklog.md (734 lines) + all 8 command MDs + page.tsx + package.json + dev.log to confirm prior session's scorecard was self-reported.
- Spun 5 parallel Explore subagents:
  - 10-a: Cross-verify cost-optimizer + 5-way intervention (7 claims)
  - 10-b: Cross-verify mandate expansion (8 claims)
  - 10-c: Cross-verify DB + Alembic + Merkle + dual-control (10 claims)
  - 10-d: Cross-verify streaming + feedback + DDM/ADWIN drift (10 claims)
  - 10-e: Cross-verify tests + CI + docs (13 claims)
- Each subagent read worklog.md first, verified file:line against actual code, produced a structured verdict table (REAL/STUB/DECORATIVE/MISSING/FALSE), appended to worklog.md, returned final report to orchestrator.
- Synthesized 5 reports into command/09-CROSS-VERIFICATION-MATRIX.md.

Stage Summary:
- HONEST SCORECARD: 34 of 48 claims are REAL (~71%). 7 PARTIAL/STUB-with-inaccurate-claim. 3 DECORATIVE. 2 FALSE/INACCURATE. 2 WEAK tests (REAL code, decorative tests). 0 MISSING.
- Real test count: 117 passed + 8 skipped = 125 total (NOT 105 as worklog claimed, NOT 93 as README/PITCH_SCRIPT say).
- 17 specific gaps identified, tiered by severity:
  - Tier 1 (production-correctness bugs, MUST fix): T1.1 dual-control HMAC chain is fake (just 2 keys required, no HMAC, no chaining); T1.2 Merkle sealing not atomic (commit before sealer.add); T1.3 test_merkle_proof_reconstructs_root has `or True` tautology; T1.4 mandate cumulative counters not persisted (reset on restart); T1.5 BoundedAgent not in production path (Mission 3 false); T1.6 test_db.py swallows ALL alembic failures with except Exception:pass; T1.7 /v1/audit/{id}/proof takes record_id:int not audit_id string.
  - Tier 2 (paper-skill application gaps, user's directive): T2.1 Bahnsen Eq.(5) per-amount FN cost NOT in 3-way decision (only in 5-way intervention); T2.2 calibrate_probabilities() Eq.(6) is dead code (imported but never called); T2.3 /v1/usage per-merchant claim is FALSE (aggregate-only); T2.4 observe_summary() Gama §5 metrics defined but never invoked; T2.5 StreamProcessor HLL doesn't drive a detector (only published as metric); T2.6 mlops.yml Stage 6+7 are echo no-ops; T2.7 RESEARCH.md DOI claim inflated (only 2 of 5 papers have DOIs).
  - Tier 3 (test coverage + doc sync): T3.1 HLL actively stubbed in tests; T3.2 no end-to-end DRIFT test via /v1/feedback/ingest; T3.3 BoundedAgent has zero test coverage; T3.4 only 3 of 5 drift gauges tested; T3.5 stale test count in 3 docs (93 vs 105 vs 117); T3.6 dashboard uses n_resamples=100 not ≥500; T3.7 no CI test that mlops.yml Stage 3 gate fires on low-PR-AUC model.
- Proposed Day 5-7 execution plan (7 tracks, 11-a through 11-g):
  - Day 5 Track P (11-a): mandate production correctness (T1.4+T1.5+T3.3)
  - Day 5 Track Q (11-b): Merkle + dual-control correctness (T1.1+T1.2+T1.3+T1.6+T1.7)
  - Day 6 Track R (11-c): Bahnsen Eq.(5)+Eq.(6) full wiring (T2.1+T2.2)
  - Day 6 Track S (11-d): Microsoft Activator parity (T2.3+T2.4)
  - Day 6 Track T (11-e): StreamProcessor HLL detector + mlops.yml honesty (T2.5+T2.6+T2.7)
  - Day 7 Track U (11-f): test coverage gaps (T3.1+T3.2+T3.4+T3.7)
  - Day 7 Track V (11-g): doc sync (T3.5+T3.6)
- Triage order if time short: cut Track V → T mlops.yml honesty → T HLL detector → S Gama §5 metrics → U fakeredis test. Never cut P/Q/R (the differentiators).
- Net effect after Days 5-7: All Tier 1 production-correctness bugs fixed. All Tier 2 paper-skill applications wired deeply. The "complex and big system that works in real" the user asked for becomes ACTUALLY real, not just claimed-real. The "doesn't look like a bloated gobble-up monster" requirement met because we're not adding new features — just making the claimed features actually do what they say.
- Tailscale bridge: still deferred. Trigger when user needs to run k6 against deployed stack OR run pytest with real Kaggle data on laptop. New project (full-stack agent payment system) acknowledged — will address after RTO submission.
- Awaiting user decision: execute Day 5-7 plan via parallel subagents, OR different priority.