/home/z/my-project/worklog.md:1137:Task: Fix 3 paper-skill application gaps in T2.1-helper (optimal_decision amount_inr), T2.2-helper (registry get_priors for Bahnsen Eq.(6) calibration), T2.4 (wire observe_summary() Gama §5 detector-quality metrics). Helper-only contract — routes.py is owned by 11-routes and was READ-ONLY for this work.
/home/z/my-project/worklog.md:1144:  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/worklog.md:1146:- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/worklog.md:1174:  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/worklog.md:1183:  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/worklog.md:1188:  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/worklog.md:1189:- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/worklog.md:1352:  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/worklog.md:1364:  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/worklog.md:1423:  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/worklog.md:1429:  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/worklog.md:1444:| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/worklog.md:1460:- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/worklog.md:1484:- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/worklog.md:1706:  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/worklog.md:1801:  * Bahnsen calibration: registry.py:70-110 (register_model accepts p_orig/p_und kwargs, folds into metrics), 113-169 (get_priors reads them), cost_optimizer.py:259-344 (calibrate_probabilities — correct direction p*p_orig/p_und, p_und=0 → 0.0, p_und==p_orig → no-op), retrain_real.py:305-324 (register_model call WITHOUT p_orig/p_und), routes.py:385-390 (lifespan also omits them), routes.py:682-690 (live path skips calibration when priors None — which is always).
/home/z/my-project/worklog.md:1808:- 9 FAIL: A1 (admin2_key is raw API string — no KDF; DB leak = total compromise), A2 (no server-side nonce store; timestamp-is-provided path has NO freshness check → captured tuple replayable forever; timestamp=None path has ~60s replay window), C8 (no SELECT FOR UPDATE; UPSERT overwrites with EXCLUDED.cumulative_monthly not increment → concurrent verifies can race past monthly cap), C9 (no month-boundary reset; cumulative_monthly read as-is regardless of updated_at), C10 (no retention/TTL prune for mandate_counter_events — alembic doc lies, actual code never DELETEs; table grows unbounded), D13 (check_agent_action accepts mandate_scope kwarg but IGNORES it — no action-to-mandate-scope binding), E14 (train.py + retrain_real.py + lifespan startup all call register_model() WITHOUT p_orig/p_und kwargs → get_priors() always returns {None, None} → calibration code path is dead in production), F17 (NO GIN/expression index on audit_records.body->>'merchant_id' — code acknowledges it's needed but migration never creates it), F19 (merchant_id is Optional on OrderIn; no middleware derives merchant_id from API key; caller-supplied, not enforced — multi-tenant isolation broken).
/home/z/my-project/worklog.md:1814:1. **The calibration code path is DEAD in production (E14 FAIL).** routes.py:682-690 calls get_priors() + checks `_priors != None` → always None → skips calibrate_probabilities. The whole T2.2 Bahnsen Eq.(6) calibration work (Track R) is a no-op in practice because no caller ever passes p_orig/p_und to register_model. The cost-optimizer is consuming the un-calibrated (inflated under sampling, except train.py doesn't use sampling) probability as-is. This is honest (no SMOTE was applied → no inflation to undo → calibration correctly no-op), but theBahnsen Eq.(6) implementation is dead code that LOOKS like a real defense.
/home/z/my-project/tool-results/read_1787826855278_2913a500c27d.txt:100:  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826855278_2913a500c27d.txt:112:  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826855278_2913a500c27d.txt:171:  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826855278_2913a500c27d.txt:177:  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826855278_2913a500c27d.txt:192:  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787832416195_7042be2670ed.txt:183:   183→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:51:    51→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:63:    63→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:122:   122→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:128:   128→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:143:   143→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:159:   159→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787826860185_fccf34a611bd.txt:183:   183→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:4:  1144→  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:6:  1146→- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:34:  1174→  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:43:  1183→  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:48:  1188→  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/read_1787825297657_1f36af035bf0.txt:49:  1189→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:115:   115→  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:127:   127→  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:186:   186→  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:192:   192→  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:207:   207→  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:223:   223→  1460→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787826849423_2bc0a50e0321.txt:247:   247→  1484→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787826856889_df03b5857ae6.txt:100:   100→  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826856889_df03b5857ae6.txt:112:   112→  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826856889_df03b5857ae6.txt:171:   171→  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826856889_df03b5857ae6.txt:177:   177→  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826856889_df03b5857ae6.txt:192:   192→  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787832409322_1c99fdeb6d88.txt:184:  1706→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1137:  1137→Task: Fix 3 paper-skill application gaps in T2.1-helper (optimal_decision amount_inr), T2.2-helper (registry get_priors for Bahnsen Eq.(6) calibration), T2.4 (wire observe_summary() Gama §5 detector-quality metrics). Helper-only contract — routes.py is owned by 11-routes and was READ-ONLY for this work.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1144:  1144→  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1146:  1146→- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1174:  1174→  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1183:  1183→  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1188:  1188→  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1189:  1189→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1352:  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1364:  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1423:  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1429:  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1444:  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1460:  1460→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1484:  1484→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1706:  1706→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1801:  1801→  * Bahnsen calibration: registry.py:70-110 (register_model accepts p_orig/p_und kwargs, folds into metrics), 113-169 (get_priors reads them), cost_optimizer.py:259-344 (calibrate_probabilities — correct direction p*p_orig/p_und, p_und=0 → 0.0, p_und==p_orig → no-op), retrain_real.py:305-324 (register_model call WITHOUT p_orig/p_und), routes.py:385-390 (lifespan also omits them), routes.py:682-690 (live path skips calibration when priors None — which is always).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1808:  1808→- 9 FAIL: A1 (admin2_key is raw API string — no KDF; DB leak = total compromise), A2 (no server-side nonce store; timestamp-is-provided path has NO freshness check → captured tuple replayable forever; timestamp=None path has ~60s replay window), C8 (no SELECT FOR UPDATE; UPSERT overwrites with EXCLUDED.cumulative_monthly not increment → concurrent verifies can race past monthly cap), C9 (no month-boundary reset; cumulative_monthly read as-is regardless of updated_at), C10 (no retention/TTL prune for mandate_counter_events — alembic doc lies, actual code never DELETEs; table grows unbounded), D13 (check_agent_action accepts mandate_scope kwarg but IGNORES it — no action-to-mandate-scope binding), E14 (train.py + retrain_real.py + lifespan startup all call register_model() WITHOUT p_orig/p_und kwargs → get_priors() always returns {None, None} → calibration code path is dead in production), F17 (NO GIN/expression index on audit_records.body->>'merchant_id' — code acknowledges it's needed but migration never creates it), F19 (merchant_id is Optional on OrderIn; no middleware derives merchant_id from API key; caller-supplied, not enforced — multi-tenant isolation broken).
/home/z/my-project/tool-results/read_1787837790982_1f1038d6cf5a.txt:1814:  1814→1. **The calibration code path is DEAD in production (E14 FAIL).** routes.py:682-690 calls get_priors() + checks `_priors != None` → always None → skips calibrate_probabilities. The whole T2.2 Bahnsen Eq.(6) calibration work (Track R) is a no-op in practice because no caller ever passes p_orig/p_und to register_model. The cost-optimizer is consuming the un-calibrated (inflated under sampling, except train.py doesn't use sampling) probability as-is. This is honest (no SMOTE was applied → no inflation to undo → calibration correctly no-op), but theBahnsen Eq.(6) implementation is dead code that LOOKS like a real defense.
/home/z/my-project/tool-results/read_1787832412229_fae80ad22912.txt:184:   184→  1706→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:51:  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:63:  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:122:  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:128:  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:143:| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:159:- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/bash_1787826858715_7cf6af1d3c0b.txt:183:- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:5:  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:7:- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:35:  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:44:  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:49:  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/bash_1787825301826_8e30c7e59810.txt:50:- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:5:     5→  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:7:     7→- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:35:    35→  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:44:    44→  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:49:    49→  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/read_1787825304945_cf99fb7d38f7.txt:50:    50→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/command/11-KAGGLE-TRAINING-PROMPT.md:114:E14 = "train.py doesn't pass priors → calibration dead". The root cause: the current training pipeline (whatever produced the existing `model.pkl`) didn't compute/store `p_orig` + `p_und`. This prompt forces the new training script to emit `priors.json` as a first-class artifact AND (optionally) call `register_model(priors=...)` so the registry's `get_priors()` returns real values. Once that lands, `calibrate_probabilities(p, p_orig, p_und)` in `registry.py:157` actually rescales the probability, and the cost-optimizer's Bahnsen Eq. 5/6 math becomes live.
/home/z/my-project/command/10-RE-SCORECARD.md:44:| **T2.2** | `calibrate_probabilities()` was DEAD CODE | Imported at routes.py:32, never called | `get_priors()` + `calibrate_probabilities()` called before `optimal_decision()` | `routes.py:669` `_priors = get_priors()` + `routes.py:675` `proba = calibrate_probabilities(...)`. `registry.py` has `get_priors()` method returning `{"p_orig":..., "p_und":...}`. No-op when priors are None (current model — correct, no resampling was done). |
/home/z/my-project/command/10-RE-SCORECARD.md:169:14. **Prior computation**: `get_priors()` returns `p_orig` / `p_und`. For the CURRENT model (trained without resampling), both are None → calibration is a no-op. But when the user retrain_real on Kaggle data (which IS imbalanced), will the training pipeline actually COMPUTE and STORE these priors? Or will they still be None because the `train.py` script doesn't write them?
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:114:   114→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:126:   126→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:185:   185→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:191:   191→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:206:   206→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:222:   222→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787826852129_0a42ea56926e.txt:246:   246→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1137:  1137→Task: Fix 3 paper-skill application gaps in T2.1-helper (optimal_decision amount_inr), T2.2-helper (registry get_priors for Bahnsen Eq.(6) calibration), T2.4 (wire observe_summary() Gama §5 detector-quality metrics). Helper-only contract — routes.py is owned by 11-routes and was READ-ONLY for this work.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1144:  1144→  * `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1146:  1146→- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1174:  1174→  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1183:  1183→  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1188:  1188→  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1189:  1189→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1352:  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1364:  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1423:  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1429:  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1444:  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1460:  1460→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1484:  1484→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1706:  1706→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1801:  1801→  * Bahnsen calibration: registry.py:70-110 (register_model accepts p_orig/p_und kwargs, folds into metrics), 113-169 (get_priors reads them), cost_optimizer.py:259-344 (calibrate_probabilities — correct direction p*p_orig/p_und, p_und=0 → 0.0, p_und==p_orig → no-op), retrain_real.py:305-324 (register_model call WITHOUT p_orig/p_und), routes.py:385-390 (lifespan also omits them), routes.py:682-690 (live path skips calibration when priors None — which is always).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1808:  1808→- 9 FAIL: A1 (admin2_key is raw API string — no KDF; DB leak = total compromise), A2 (no server-side nonce store; timestamp-is-provided path has NO freshness check → captured tuple replayable forever; timestamp=None path has ~60s replay window), C8 (no SELECT FOR UPDATE; UPSERT overwrites with EXCLUDED.cumulative_monthly not increment → concurrent verifies can race past monthly cap), C9 (no month-boundary reset; cumulative_monthly read as-is regardless of updated_at), C10 (no retention/TTL prune for mandate_counter_events — alembic doc lies, actual code never DELETEs; table grows unbounded), D13 (check_agent_action accepts mandate_scope kwarg but IGNORES it — no action-to-mandate-scope binding), E14 (train.py + retrain_real.py + lifespan startup all call register_model() WITHOUT p_orig/p_und kwargs → get_priors() always returns {None, None} → calibration code path is dead in production), F17 (NO GIN/expression index on audit_records.body->>'merchant_id' — code acknowledges it's needed but migration never creates it), F19 (merchant_id is Optional on OrderIn; no middleware derives merchant_id from API key; caller-supplied, not enforced — multi-tenant isolation broken).
/home/z/my-project/tool-results/read_1787837790695_1f1038d6cf5a.txt:1814:  1814→1. **The calibration code path is DEAD in production (E14 FAIL).** routes.py:682-690 calls get_priors() + checks `_priors != None` → always None → skips calibrate_probabilities. The whole T2.2 Bahnsen Eq.(6) calibration work (Track R) is a no-op in practice because no caller ever passes p_orig/p_und to register_model. The cost-optimizer is consuming the un-calibrated (inflated under sampling, except train.py doesn't use sampling) probability as-is. This is honest (no SMOTE was applied → no inflation to undo → calibration correctly no-op), but theBahnsen Eq.(6) implementation is dead code that LOOKS like a real defense.
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:64:  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:76:  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:135:  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:141:  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:156:| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:172:- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/bash_1787826851121_5d0466c5d576.txt:196:- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:114:  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:126:  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:185:  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:191:  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:206:| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:222:- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/bash_1787826850261_0cd9955af95f.txt:246:- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:115:  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:127:  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:186:  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:192:  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:207:  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:223:  1460→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787826848421_df7da75977c0.txt:247:  1484→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:115:  1352→  * src/ml/registry.py get_priors(model_version=None, registry_path=...) returns {"p_orig": float|None, "p_und": float|None} (11-c, lines 113-169). Returns None/None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them).
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:127:  1364→  * Reads `get_priors()` (defaults to current champion).
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:186:  1423→  - T2.2: `get_priors()` returns None/None for the live in-process model (no priors passed at registration time) → calibrate_probabilities is a no-op (correct when no SMOTE was applied).
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:192:  1429→  * `src/api/routes.py` (1984 → 2200 lines, +216): all 6 fixes integrated. Top-level imports added (hashlib, hmac, json, Depends, Query, get_priors, ALLOWED_ACTIONS, check_agent_action). OrderIn.merchant_id field added. OverrideIn.timestamp field added + docstring updated for HMAC chain. LabelFeedbackService construction now passes metrics=state["metrics"] (Metrics() moved to module-load time before LabelFeedbackService). /risk/score decision layer calls optimal_decision(proba, amount_inr=order.amount_inr, **DEFAULT_COST_WEIGHTS) + calibrate_probabilities when priors differ; policy_hint mirrors the per-amount cost model; audit.log payload includes merchant_id; response body exposes audit_id top-level. /v1/simulate mirrors the same T2.1+T2.2 changes. /v1/audit/{audit_id}/proof accepts string audit_id (was record_id: int), translates via _lookup_record_id_by_audit_id helper. /v1/usage accepts merchant_id query param + calls _usage_counts_per_merchant helper when provided. POST /risk/score, /v1/mandates, /risk/{pid}/override now have dependencies=[Depends(enforce_agent_action)]. /risk/{pid}/override dual-control path rewritten with real HMAC chain verification (iterate admin keys, compute HMAC, compare to admin_signature_2 via hmac.compare_digest; 403 on no match; audit stores admin_signature_1_digest + admin_signature_2_hmac_chain + dual_control_chain_verified + dual_control_timestamp; response surfaces dual_control_chain_verified + dual_control_timestamp). Module-level helpers added: enforce_agent_action (FastAPI dependency), _lookup_record_id_by_audit_id (audit_id → record_id translator), _usage_counts_per_merchant (per-merchant JSONB filter).
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:207:  1444→| T2.2 | calibrate_probabilities imported but never called (dead code) | Added `_priors = get_priors()` + `if priors differ: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]` before optimal_decision. Applied in /risk/score + /v1/simulate mirror. No-op when priors are both-None (live in-process model registered without priors → correct when no SMOTE was applied) | routes.py:~630-638 (/risk/score), ~1795-1803 (/v1/simulate) | **REAL** |
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:223:  1460→- The live in-process model (registered at routes.py:298-303 in the lifespan) doesn't pass p_orig/p_und to register_model() (the pre-Track-R registration flow). So `get_priors()` returns None/None → the live path skips calibration → the un-calibrated probability is used as-is. This is CORRECT for the current setup (the in-process model is trained on the original distribution, no SMOTE/under-sampling was applied → no calibration needed). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE, the priors can be computed there + passed in the register_model call → the live path will automatically calibrate (the wiring is in place via T2.2).
/home/z/my-project/tool-results/read_1787826848063_df7da75977c0.txt:247:  1484→- src/ml/registry.py (owned by 11-c) — consumed get_priors() at the public interface.
/home/z/my-project/tool-results/bash_1787825299834_ae20d0000f37.txt:12:  1174→  * `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/bash_1787825299834_ae20d0000f37.txt:21:  1183→  2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/bash_1787825299834_ae20d0000f37.txt:26:  1188→  3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/bash_1787825299834_ae20d0000f37.txt:27:  1189→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/tool-results/read_1787832409071_f9c9764a6b19.txt:184:  1706→  - T2.2: routes.py:669 get_priors + 675 calibrate_probabilities → CALIBRATION WIRED
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:5:     5→* `get_priors(model_version=None, registry_path="out/model_registry.json") -> {"p_orig": float|None, "p_und": float|None}` — returns the stored Bahnsen Eq.(6) priors. Defaults to the current champion when `model_version` is None. Returns both-None when (a) no model is registered OR (b) the model was registered WITHOUT priors (the pre-Track-R registration flow at routes.py:298-303 doesn't pass them — the live in-process HistGB's priors are unknown, so 11-routes should skip calibration, which is the same as Track C's behaviour, correct when no SMOTE/under-sampling was applied).
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:7:     7→- Verified get_priors() correctness with a Python smoke test in file mode: register v1 without priors → `get_priors() == {"p_orig": None, "p_und": None}`; register v2 with p_orig=0.0467, p_und=0.50 as champion → `get_priors() == {"p_orig": 0.0467, "p_und": 0.50}`; `get_priors("v1")` returns None/None (pre-Track-R entries); `get_priors("v999")` returns None/None (missing version).
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:34:    34→* `src/ml/registry.py` (+92 lines net): added `p_orig`/`p_und` kwargs to `register_model()`, added `get_priors()` public function, added `_get_model_by_version()` + `_get_model_postgres()` internal helpers.
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:43:    43→2. `priors = get_priors()` (default reads current champion) — returns `{"p_orig": float|None, "p_und": float|None}`. 11-routes wraps the calibration call: `if priors.get("p_orig") is not None and priors.get("p_und") is not None: proba = calibrate_probabilities([proba], priors["p_orig"], priors["p_und"])[0]`.
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:48:    48→3. Optionally: at routes.py:298-303 (the lifespan register_model call), pass `p_orig=<original training prior>`, `p_und=<post-resampling training prior>` from the train_df metadata. Currently the live registration doesn't pass priors — `get_priors()` returns None/None, the live path skips calibration (correct for the in-process HistGB trained on the original distribution, no SMOTE applied). When the model training pipeline (scripts/retrain_real.py) is extended to use SMOTE/under-sampling, the priors can be computed there and passed in the register_model call.
/home/z/my-project/tool-results/read_1787825308714_dfadd20497ff.txt:49:    49→- One-liner cross-check: `python -c "from src.business.cost_optimizer import optimal_decision; from src.ml.registry import get_priors; from src.feedback.label_service import LabelFeedbackService; assert optimal_decision(0.4, amount_inr=600)[0] != optimal_decision(0.4, amount_inr=52000)[0]; assert set(get_priors().keys()) == {'p_orig', 'p_und'}; s = LabelFeedbackService(metrics=None); assert s._prev_combined_state == 'STABLE'"` — passes.
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/ml/registry.py:101:        are unchanged. :func:`get_priors` reads them back out.
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/ml/registry.py:113:def get_priors(
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/ml/registry.py:155:        priors = get_priors()  # or get_priors(champ["version"])
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/ml/registry.py:323:    Day 6 Track R (T2.2-helper) — used by :func:`get_priors` to read the
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/api/routes.py:120:    get_priors,
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/api/routes.py:781:                    #    done). ``get_priors()`` returns both-None when the
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/api/routes.py:787:                    _priors = get_priors()
/home/z/my-project/upload/RTO_Trust_Layer_FULL/src/api/routes.py:2464:                    _priors = get_priors()