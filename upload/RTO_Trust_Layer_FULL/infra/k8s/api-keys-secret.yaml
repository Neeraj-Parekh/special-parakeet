# Template secret for API auth keys — REPLACE the base64 values before
# `kubectl apply -f`. These are demo keys only; rotate for production.
apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  namespace: rto-trust-layer
  labels:
    app.kubernetes.io/name: rto-trust-layer
    app.kubernetes.io/component: api
type: Opaque
stringData:
  scorer: score-demo-key
  admin: admin-demo-key
  mandate: ci-secret
  salt: ci-salt
