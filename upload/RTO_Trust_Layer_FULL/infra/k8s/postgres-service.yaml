apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: rto-trust-layer
  labels:
    app.kubernetes.io/name: rto-trust-layer
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
  selector:
    app.kubernetes.io/name: rto-trust-layer
    app.kubernetes.io/component: database
